% !TEX root = main.tex

% --- JAZYK A KÓDOVÁNÍ ---
\usepackage[main=czech]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}

% --- ZÁKLADNÍ BALÍČKY ---
\usepackage{geometry}
\geometry{margin=2.5cm, headheight=15pt}
\usepackage{fancyhdr}
\usepackage{parskip}
\linespread{1.1}

% --- POKROČILÉ BALÍČKY PRO MATEMATIKU ---
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{bm}
\allowdisplaybreaks[3]

% --- BARVY A GRAFIKA ---
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{float}
\usepackage{subcaption}
\graphicspath{{figures/}{images/}}

% --- POKROČILÉ TABULKY A ALGORITMY ---
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{algorithm}
\usepackage{algpseudocode}

% --- TCOROLORBOX PRO BLOKY ---
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable, theorems}

% --- TIKZ PRO DIAGRAMY ---
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc, patterns}
\usetikzlibrary{decorations.pathreplacing, calligraphy}

% --- HYPERREF A CLEVEREF ---
\usepackage{hyperref}
\makeatletter
\@ifundefined{ver@cleveref.sty}{%
  \newcommand{\cref}[1]{\ref{#1}}
  \newcommand{\Cref}[1]{\ref{#1}}
}{%
  \usepackage[capitalize,nameinlink]{cleveref}
}
\makeatother

% --- LISTINGS PRO KÓD ---
\usepackage{listings}

% --- BIBLIOGRAFIE ---
\usepackage[style=iso-numeric, autolang=other]{biblatex}